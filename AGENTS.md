# Agent Notes

- Do not start the development server for this repo. The user runs it.
- When browser-checking the app, use the user's existing server at `http://localhost:3000`.
- Never run `npm run build-lite` for checks. Finish the requested edits and leave verification to the user.
